{
  "name": "resrvd",
  "version": "1.0.0",
  "main": "index.js",
  "license": "Apache-2.0",
  "private": true,
    "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace web start",
    "build:packages": "npx tsc -b packages",
    "build:packages:watch": "npx tsc -b packages --watch --verbose"
  }
}
